Run /cca-init first (full startup sequence), then immediately run /cca-auto-worker (CLI worker autonomous mode).

Do not pause between them. Do not ask for confirmation. This is a combined launcher — execute both in sequence exactly as if the user typed them separately.
